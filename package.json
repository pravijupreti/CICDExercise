{
  "name": "coc-project",
  "private": true,
  "scripts": {
    "lint": "eslint .",
    "test": "echo \"No tests yet\"",
    "build": "npm --prefix frontend run build"
  },
  "devDependencies": {
    "eslint": "^10.1.0"
  }
}
